function flag = isPositiveInteger(x)
%function to validate input attributes used in inputParser: return true/false
%Check if input is a positive scalar

flag = isnumeric(x) && isscalar(x) && (x>0) && (x==round(x)); 

end
